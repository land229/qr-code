<form method="post" action="{{ path('app_admin_scan_delete', {'id': scan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scan.id) }}">
    <button class="btn">Delete</button>
</form>
